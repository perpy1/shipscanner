import { ScrapedPost } from "@/types";

// Product Hunt requires an API token for proper access.
// Stub for MVP — returns empty array.
export async function scrapeProductHunt(): Promise<ScrapedPost[]> {
  // TODO: Implement with Product Hunt API token
  return [];
}
