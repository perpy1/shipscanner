export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-[1080px] px-8 pt-10 pb-14 border-t border-[var(--line)] flex flex-wrap items-center justify-between gap-4 text-[13px] text-[var(--mute)]">
      <span className="font-head text-[17px] text-[var(--text)]">SideQuest</span>
      <span>No signup · saves locally · a new drop every morning</span>
      <span>Fresh ideas distilled daily</span>
    </footer>
  );
}
